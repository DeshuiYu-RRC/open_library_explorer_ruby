<!DOCTYPE html>
<html lang="en">
<head>
  <title>Open Library Explorer</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap CSS (Feature 5.3) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <style>
      body {
          padding-top: 70px;
      }
      .book-cover {
          max-width: 100%;
          height: auto;
          box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      }
      .star-rating {
          color: #ffc107;
      }
      .subject-badge {
          margin: 2px;
      }
  </style>
</head>

<body>
<!-- Navigation Menu (Feature 2.2) -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <%= link_to "Open Library Explorer", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Books", books_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Authors", authors_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link" %>
        </li>
      </ul>

      <!-- Search Form (Feature 4.1) -->
      <%= form_with(url: search_books_path, method: :get, class: "d-flex") do |f| %>
        <%= f.text_field :query,
                         placeholder: "Search books...",
                         class: "form-control me-2",
                         value: params[:query] %>
        <%= f.submit "Search", class: "btn btn-outline-light" %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Main Content -->
<div class="container mt-4">
  <%= yield %>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>